/**
 * User Type Constants
 * Defines the two types of users in the system
 */

export const USER_TYPES = {
  NORMAL_USER: 'NORMAL_USER',
  ADMIN: 'ADMIN'
};

export const DEFAULT_USER_TYPE = USER_TYPES.NORMAL_USER;
